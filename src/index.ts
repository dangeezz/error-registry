/**
 * Error Registry - A configurable error handling system with registry pattern.
 * 
 * Provides flexible error code lookup, custom error classes, and global error handlers.
 * 
 * @packageDocumentation
 */

export { createErrorRegistry } from './errorRegistry';
export type { Config, ErrorRegistry } from './errorRegistry';
